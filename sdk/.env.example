# Template for sdk/.env (the real file is gitignored). Fill in your own keys; never commit secrets.
VENICE_API_KEY=
VENICE_MODEL=qwen3-6-27b
PRIVATE_KEY=
# Pinned agent/worker/policy EOAs - stable so signed delegations stay valid across restarts.
# Agent + worker gas is auto-topped from the deployer (PRIVATE_KEY).
AGENT_PRIVATE_KEY=
WORKER_PRIVATE_KEY=
POLICY_PRIVATE_KEY=
