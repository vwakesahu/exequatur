# deps
node_modules/
sdk/node_modules/

# env / secrets
.env
.env.local
*.key

# foundry
contracts/out/
contracts/cache/
contracts/broadcast/**/dry-run/
# deps are installed by pinned tag via contracts/install-deps.sh (not vendored)
contracts/lib/

# ts build
sdk/dist/
*.tsbuildinfo

# logs / os
*.log
.DS_Store

# docs .md kept local; only the architecture image is published
docs/*
!docs/arc.png

# runtime spike state (full tx hashes, ephemeral addresses)
last-run.json
